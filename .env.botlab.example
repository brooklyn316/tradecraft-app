# ── Supabase (Bot Lab project — NOT the live Tradecraft project) ──
NEXT_PUBLIC_SUPABASE_URL=https://your-botlab-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ── Vercel Cron auth (generate any random string) ─────────────
CRON_SECRET=your-random-secret-here

# ── External data APIs (Group E bots) ────────────────────────
# Quiver Quantitative — free tier available at quiverquant.com
QUIVER_QUANT_API_KEY=your-quiver-key

# SEC EDGAR — free, no key needed (uses User-Agent header)
# ARK Invest CSV — free, no key needed
